# Origin

Extracted from `Environment/chatroom` in the historical Python workbench known locally as `the real thing`.

The source backend was built in June 2026 to coordinate durable multi-participant rooms without making a chat UI or model context window the source of truth. This repository removes host registry coupling, private generated rooms, model-specific connectors, and personal fixtures while preserving the implemented storage and governance behavior.
